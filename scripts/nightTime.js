const toggleNight = (map, player) => {
  // const darkeningFilter = new PIXI.ColorMatrixFilter();
  // darkeningFilter.brightness(0.5); // Adjust the brightness to make it darker
  // // Apply the darkening filter to the map container
  // map.map.filters = [darkeningFilter];
  // map.mapLayer2.filters = [darkeningFilter];
};

export default toggleNight;
